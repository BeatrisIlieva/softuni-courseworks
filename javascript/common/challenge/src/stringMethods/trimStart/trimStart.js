"use strict";

// The trimStart() method removes white spaces at the beginning of astring

const text = "    Unnecessary spaces    ";

const trimmedText = text.trimStart();

// expected "Unnecessary spaces    "

console.log(trimmedText);
