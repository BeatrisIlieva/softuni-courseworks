"use strict";

// The Split() method returns an arrays with elements in a string splitting them by a given separator

const sentences = "We love JavaScript";

const words = sentences.split(" ");

// expected ["We", "love", "JavaScript"]
console.log(words);
