"use strict";

// The unshift() method adds one or more elements at the beginning of an array; Note that this method modifies the array

const array = [1, 2, 3, 4, 5];

const firstNumber = array.unshift(-1, 0);

console.log(array);
console.log(array.length);
