"use strict";

// The join() method returns a new string by concatenating all of the elements in an array separated by a specified string separator

const array = ["Cats", "are", "amazing", "creatures", "!"];

const sentence = array.join(" ");

// expected "Cats are amazing creatures!"

console.log(sentence);
