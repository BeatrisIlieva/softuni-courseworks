"use strict";

// The push() method adds one or more elements to the end of an array; Note that this method modifies the array

const array = [1, 2, 3, 4, 5];

const lastNumber = array.push(6, 7);

console.log(array);
console.log(array.length);
