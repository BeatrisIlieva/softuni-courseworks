/*
    In JS we have expressions and statements.
    Expressions end with semi-column ;
    Statements don't
*/

// Expression
const a = 5;
const b = 7;

// Statement
if (a > b) { // block start
    // Expression
    console.log(true);
} // block end
else {
    // Expression
    console.log(false);
}
