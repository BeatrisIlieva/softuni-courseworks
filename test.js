const result = [
    [ 'CA9999TT', true ],
    [ 'CA2844AA', true ],
    [ 'CA9876HH', true ],
    [ 'CA2822UU', true ]
  ]

// console.log(JSON.stringify(result.sort((a, b) => a[0].localeCompare(b[0]))));
// result.forEach(el => console.log(el[0]))

const res2 = {}
const resLength = Object.keys(res2).length === 0


console.log(resLength);