/*
    In JS we expressions and statements.
    Expressions end with semi-column ;
    Statements don't
*/

// Expression
const a = 5;
const b = 7;

// Statement
if (a > b) {
    // Expression
    console.log(tru);
} else {
    // Expression
    console.log(false);
}
