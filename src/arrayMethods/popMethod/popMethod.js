"use strict";

// Removes the last element from an array and returns it; Note that this method modifies the array

const array = [1, 2, 3, 4, 5];

const lastNumber = array.pop();

console.log(array);
console.log(lastNumber);
