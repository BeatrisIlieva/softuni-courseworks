"use strict";

// The shift() method removes the first element from an array; Note that this method modifies the array

const array = [1, 2, 3, 4, 5];

const firstNumber = array.shift();

console.log(array);
console.log(firstNumber);
