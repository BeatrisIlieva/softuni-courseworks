"use strict";

// The trimEnd() method removes white spaces at the end of a string

const text = "    Unnecessary spaces    ";

const trimmedText = text.trimEnd();

// expected "    Unnecessary spaces"

console.log(trimmedText);
