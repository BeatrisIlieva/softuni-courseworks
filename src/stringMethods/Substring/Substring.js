"use strict";

// The Substring() method returns a substring between start and end index; the start index is inclusive, the end index is exclusive

const sentence = "Beautiful Brazilian sky";

const substrings = sentence.substring(0, 3);

// expected Bea
console.log(substrings);
